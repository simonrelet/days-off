{
  "name": "@marv/login",
  "version": "0.1.0",
  "author": "Simon Relet",
  "private": true,
  "description": "@marv login application",
  "proxy": "http://localhost:3001",
  "scripts": {
    "build": "PUBLIC_URL=/login react-scripts build",
    "check": "eslint src",
    "clean": "rm -rf build",
    "format": "prettier --write --single-quote --trailing-comma all 'src/**/*.js'",
    "start": "react-scripts start",
    "test": "react-scripts test --env=jsdom"
  },
  "dependencies": {
    "@marv/components": "file:../components/build/",
    "prop-types": "^15.5.10",
    "qs": "^6.5.0",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "prettier": "^1.5.2"
  }
}
