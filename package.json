{
  "name": "@marv/main",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "yarn run in-packages -- yarn run build",
    "check": "yarn run in-packages -- yarn run check",
    "clean": "yarn run in-packages -- yarn run clean",
    "in-packages": "node scripts/in-packages",
    "postinstall": "yarn run in-packages -- yarn install",
    "start": "./start.sh"
  },
  "dependencies": {
    "chalk": "^2.0.1",
    "cross-spawn": "^5.1.0",
    "fs-extra": "^3.0.1",
    "minimist": "^1.2.0"
  }
}
