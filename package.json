{
  "name": "@marv/main",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "./scripts/build.sh",
    "clean": "./scripts/clean.sh",
    "heroku-postbuild": "yarn run build",
    "start": "node build"
  }
}
